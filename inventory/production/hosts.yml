all:
  children:
    jenkins:
      hosts:
        jenkins-server:
          ansible_host: 127.0.0.1
          ansible_user: root
          ansible_connection: local
          ansible_python_interpreter: /usr/bin/python3.12
