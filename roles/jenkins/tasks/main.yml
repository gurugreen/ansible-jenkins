---
- name: Validate operating system
  ansible.builtin.assert:
    that:
      - ansible_facts['distribution'] == 'Ubuntu'
      - ansible_facts['distribution_version'] is version('24.04', '==')
    fail_msg: "This role requires Ubuntu 24.04."
    success_msg: "Ubuntu 24.04 detected."

- name: Install Jenkins prerequisites
  ansible.builtin.apt:
    name:
      - fontconfig
      - openjdk-21-jre
      - python3-debian
    state: present
    update_cache: true

- name: Create APT keyring directory
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Install Jenkins repository signing key
  ansible.builtin.get_url:
    url: "{{ jenkins_repository_key_url }}"
    dest: "{{ jenkins_keyring_file }}"
    owner: root
    group: root
    mode: "0644"

- name: Configure Jenkins LTS repository
  ansible.builtin.copy:
    dest: /etc/apt/sources.list.d/jenkins.list
    owner: root
    group: root
    mode: "0644"
    content: |
      deb [signed-by={{ jenkins_keyring_file }}] https://pkg.jenkins.io/debian-stable binary/

- name: Update APT cache
  ansible.builtin.apt:
    update_cache: true
- name: Install Jenkins LTS
  ansible.builtin.apt:
    name: jenkins
    state: present
    update_cache: true

- name: Create Jenkins systemd override directory
  ansible.builtin.file:
    path: /etc/systemd/system/jenkins.service.d
    state: directory
    owner: root
    group: root
    mode: "0755"

- name: Configure Jenkins port
  ansible.builtin.copy:
    dest: /etc/systemd/system/jenkins.service.d/override.conf
    owner: root
    group: root
    mode: "0644"
    content: |
      [Service]
      Environment="JENKINS_PORT={{ jenkins_port }}"

- name: Reload systemd
  ansible.builtin.systemd_service:
    daemon_reload: true

- name: Enable and start Jenkins
  ansible.builtin.systemd_service:
    name: jenkins
    enabled: true
    state: started

- name: Wait for Jenkins
  ansible.builtin.uri:
    url: "{{ jenkins_url }}"
    status_code: 200
    timeout: 5
  register: jenkins_health
  retries: 60
  delay: 5
  until: jenkins_health.status == 200
